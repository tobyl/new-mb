---
import className from 'classnames'

interface Props {
	classes?: string;
    id?: string;
}

const { classes, id } = Astro.props;

const cls = className({
	[classes]: classes,
})
---

<section class={cls} id={id ? id : null}>
	<div class="container px-2 mx-auto">
		<slot />
	</div>
</section>
